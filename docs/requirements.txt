# 필수 라이브러리 (기본 Python에 포함됨)
# csv, time, threading, collections, sys

# 선택적 라이브러리 (실제 키보드 입력을 원할 경우)
pyautogui>=0.9.54

# 사용법:
# pip install -r requirements.txt
# 
# keyboard 라이브러리 없이도 프로그램은 정상 작동하며,
# 키 입력 시뮬레이션만 콘솔에 출력됩니다.
